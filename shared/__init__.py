"""
Shared library for SupplyChainForge microservices.

Packages:
  events/    — canonical event envelope + per-domain event schemas
  pubsub/    — thin Pub/Sub publisher and pull-subscriber wrappers
  http/      — typed async HTTP clients for inter-service calls
"""
