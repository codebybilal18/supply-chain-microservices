[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=app --cov-report=term-missing --cov-report=html:htmlcov -v"
log_cli = true
log_cli_level = "INFO"

[tool.coverage.run]
source = ["app"]
omit = ["tests/*", "alembic/*"]
